<Page x:Class="ProjectSystemHelpStudents.Views.AdminPages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
      mc:Ignorable="d" 
      Title="SettingsPage">

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="0" Margin="0,0,0,10">
            <Button x:Name="AddPriorityButton"
                    Content="Добавить приоритет"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="AddPriorityButton_Click"/>
        </StackPanel>

        <DockPanel Grid.Row="1" Margin="0,0,0,10">
            <fa:IconImage Icon="Search" Width="20" Height="20" Foreground="White" VerticalAlignment="Center"/>
            <TextBox x:Name="PrioritySearchBox"
                     Style="{StaticResource InputFieldStyle}"
                     Width="200"
                     Margin="6,0,0,0"
                     TextChanged="PrioritySearchBox_TextChanged"/>
        </DockPanel>

        <DataGrid x:Name="PriorityGrid"
                  Grid.Row="2"
                  Style="{StaticResource DarkDataGrid}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*" />

                <DataGridTemplateColumn Header="Действия" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
                                <Button Content="✎"
                                        ToolTip="Изменить"
                                        Style="{StaticResource TransparentButtonStyle}"
                                        Click="EditPriorityButton_Click"
                                        Tag="{Binding PriorityId}" />
                                <Button Content="✕"
                                        ToolTip="Удалить"
                                        Style="{StaticResource TransparentButtonStyle}"
                                        Click="DeletePriorityButton_Click"
                                        Tag="{Binding PriorityId}" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
